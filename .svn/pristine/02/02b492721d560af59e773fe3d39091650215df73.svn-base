var Ball = function(){
}

Ball.prototype.draw_ball = function(x,y,r) {
    context.beginPath();
    context.arc(x, y, r, 0, Math.PI*2, true);
    context.closePath();
    context.fill();
}
Ball.prototype.ballRadius = 10;